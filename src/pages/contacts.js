import React from 'react';
import Layout from './../components/Layout';

const Contacts= () => (
  <Layout>
    <div className="contacts">
      <h2>Contacts</h2>
      <p>Here the contacts</p>
      <div className="map">Map?</div>
    </div>
  </Layout>
);

export default Contacts;